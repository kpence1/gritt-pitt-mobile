import {Component} from '@angular/core';
import {NavController} from 'ionic-angular';

@Component({
  templateUrl: 'build/pages/clients/clients.html'
})
export class ClientsPage {
  constructor(private nav: NavController) {
  }
}
